# AGENTS.md

## Cursor Cloud specific instructions

### Repository state

This repository ("Persian_RTL_for_GPTs") is a stub project intended to fix mixed RTL characters of Persian language in ChatGPT, Perplexity, Claude, Gemini, etc. The `.gitignore` uses a Node.js template, suggesting the project will be built with Node.js/TypeScript.

As of initial setup, the repo contains only `README.md`, `.gitignore`, and `LICENSE` — no source code, dependencies, or services.

### Development environment

- **Runtime:** Node.js (available via nvm; v22 LTS installed by default)
- **Package manager:** Not yet determined (no lockfile exists). The `.gitignore` includes entries for npm, yarn, and pnpm. Choose whichever matches any future lockfile.
- **No services to start** — when application code is added, update this section with startup instructions.

### Lint / Test / Build / Run

No commands available yet. When `package.json` is added, standard scripts (e.g., `npm run lint`, `npm test`, `npm run dev`) should be documented here.
